# Environment Variables for FIQ Supply Chain Control Tower
# Copy this file to .env.local (ignored by git) and fill in the values.

# Google Gemini API Key (required for AI analyst features)
# For production builds, use VITE_GEMINI_API_KEY (Vite exposes VITE_ prefixed vars to client)
VITE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Legacy support (for development)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Optional: Adjust API model (fallback handled in code)
# GEMINI_MODEL=gemini-2.5-flash
